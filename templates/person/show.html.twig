{% extends 'base.html.twig' %}

{% block title %}Person{% endblock %}

{% block body %}
    <h1>Person</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Personid</th>
                <td>{{ person.personid }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ person.firstname }}</td>
            </tr>
            <tr>
                <th>Middlenames</th>
                <td>{{ person.middlenames }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ person.lastname }}</td>
            </tr>
            <tr>
                <th>Academictitle</th>
                <td>{{ person.academictitle }}</td>
            </tr>
            <tr>
                <th>Photourl</th>
                <td>{{ person.photourl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('person_index') }}">back to list</a>

    <a href="{{ path('person_edit', {'personid': person.personid}) }}">edit</a>

    {{ include('person/_delete_form.html.twig') }}
{% endblock %}

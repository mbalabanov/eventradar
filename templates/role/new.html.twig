{% extends 'base.html.twig' %}

{% block title %}New Role{% endblock %}

{% block body %}
<div class="row">
    <div class="col-md-8 offset-md-2">
        <h2>Add New Role</h2>
        <p><a href="{{ path('role_index') }}" class="btn btn-primary">Back to Roles</a></p>
        {{ include('role/_form.html.twig') }}
    </div>
</div>

{% endblock %}

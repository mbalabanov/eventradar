{% extends 'base.html.twig' %}

{% block title %}New Country{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-10 offset-md-1">
            <h2>Add Country</h2>
            <p><a href="{{ path('country_index') }}" class="btn btn-primary">Back to Countries</a></p>
            {{ include('country/_form.html.twig') }}
        </div>
    </div>
{% endblock %}

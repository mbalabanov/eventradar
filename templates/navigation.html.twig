<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/"><img src="/assets/eventradar.png" alt="EventRadar" height="60px" class="mx-4"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('default_index') }}">Future Events</a>
                </li>
                <li>
                    <a class="nav-link" href="{{ path('pastevents_index') }}">Past Events</a>
                </li>
            </ul>

            {% if not app.user %}
                <a href="/login" class="btn btn-light m-2">Login</a>
                <a href="/register" class="btn btn-light m-2">Register</a>
            {% endif %}

            {% if app.user %}
                <div class="dropdown mx-1">
                    <button class="btn btn-light dropdown-toggle" type="button" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        Admin
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="userDropdown">
                        <li><a class="dropdown-item" href="{{ path('default_index') }}">Events</a></li>
                        <li><a class="dropdown-item" href="{{ path('location_index') }}">Locations</a></li>
                        <li><a class="dropdown-item" href="{{ path('team_index') }}">Teams</a></li>
                        <li><a class="dropdown-item" href="{{ path('person_index') }}">Persons</a></li>
                        <li><a class="dropdown-item" href="{{ path('role_index') }}">Roles</a></li>
                        <li><a class="dropdown-item" href="{{ path('league_index') }}">Leagues</a></li>
                        <li><a class="dropdown-item" href="{{ path('country_index') }}">Countries</a></li>
                        <li><a class="dropdown-item" href="{{ path('contest_index') }}">Contest</a></li>
                        <li><a class="dropdown-item" href="{{ path('category_index') }}">Sports Categories</a></li>
                    </ul>
                </div>
                <a href="/logout" class="btn btn-light m-2">Logout</a>
            {% endif %}

        </div>
    </div>
</nav>